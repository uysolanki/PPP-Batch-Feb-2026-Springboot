Why Java 8 Date-Time API?

Before Java 8, we had:

java.util.Date → mutable, confusing
java.util.Calendar → overly complicated
SimpleDateFormat → not thread-safe

Java 8 solved all these problems with a new package:

✅ java.time

It is:
Immutable ✔
Thread-safe ✔
Readable ✔
Inspired by Joda-Time ✔

Clean API for handling dates, time, time zones, periods, durations ✔

🌟 Main Classes in java.time
Purpose						Class				Example
Date only					LocalDate			2025-11-14
Time only					LocalTime			10:30:15
Date + Time					LocalDateTime		2025-11-14T10:30
Date + Time + Zone			ZoneDateTime		2025-11-14T10:30+05:30
Represents a time zone		ZoneId				Asia/Kolkata
Instant on timeline (UTC)	Instant				timestamp
Text formatting				DateTimeFormatter	patterns
Distance in dates			Period				2 years, 3 months
Distance in time			Duration			120 seconds


🚀 1. Getting Current Date, Time, DateTime
Current Date
LocalDate date = LocalDate.now();
System.out.println(date);   // 2025-11-14

Current Time
LocalTime time = LocalTime.now();
System.out.println(time);   // 10:45:30

Current DateTime
LocalDateTime dt = LocalDateTime.now();
System.out.println(dt);

🚀 2. Creating Specific Dates and Times
Specific Date
LocalDate d = LocalDate.of(2025, 11, 13);

Specific Time
LocalTime t = LocalTime.of(10, 30, 45);

Specific DateTime
LocalDateTime dt = LocalDateTime.of(2025, 11, 13, 10, 30);

🚀 3. Adding / Subtracting Dates
LocalDate today = LocalDate.now();
LocalDate tomorrow = today.plusDays(1);
LocalDate previousMonth = today.minusMonths(1);

🚀 4. Formatting Dates (Very Important)
LocalDate date = LocalDate.now();

String formatted = date.format(DateTimeFormatter.ofPattern("dd-MM-yyyy"));
System.out.println(formatted);

Example formats:
"dd-MM-yyyy"
"yyyy/MM/dd"
"dd MMM yyyy"
"EEEE, dd MMMM yyyy"

🚀 5. Parsing Dates (String → Date)
String s = "14-11-2025";
LocalDate date = LocalDate.parse(s, DateTimeFormatter.ofPattern("dd-MM-yyyy"));

🚀 6. Working with Time Zones
ZoneId zone = ZoneId.of("Asia/Kolkata");
ZonedDateTime zdt = ZonedDateTime.now(zone);

System.out.println(zdt);

Convert from one zone to another:
ZonedDateTime india = ZonedDateTime.now(ZoneId.of("Asia/Kolkata"));
ZonedDateTime usa = india.withZoneSameInstant(ZoneId.of("America/New_York"));

System.out.println(usa);

🚀 7. Instant (Timestamp Like in Databases)
Instant instant = Instant.now();
System.out.println(instant);


Used for:

Timestamps

Logging

Storing in databases

🚀 8. Period vs Duration
Period → Difference in dates (Years, Months, Days)
LocalDate start = LocalDate.of(2020, 1, 1);
LocalDate end = LocalDate.of(2025, 1, 1);

Period p = Period.between(start, end);
System.out.println(p.getYears());  // 5

Duration → Difference in time (Hours, Minutes, Seconds)
LocalTime t1 = LocalTime.of(10, 0);
LocalTime t2 = LocalTime.of(12, 30);

Duration d = Duration.between(t1, t2);
System.out.println(d.toHours()); // 2
System.out.println(d.toMinutes()); // 150

🚀 9. Converting Between Old and New APIs
Date → Instant → LocalDateTime
Date date = new Date();
Instant instant = date.toInstant();
LocalDateTime dt = LocalDateTime.ofInstant(instant, ZoneId.systemDefault());

LocalDateTime → Date
LocalDateTime ldt = LocalDateTime.now();
Date date = Date.from(ldt.atZone(ZoneId.systemDefault()).toInstant());